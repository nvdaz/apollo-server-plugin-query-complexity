{
  "name": "apollo-server-plugin-query-complexity",
  "version": "0.0.1",
  "description": "Apollo server plugin that limits query complexity.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "nvdaz/apollo-server-query-complexity-plugin"
  },
  "peerDependencies": {
    "graphql": "^15.0.0",
    "graphql-query-complexity": "^0.7.0"
  },
  "dependencies": {
    "apollo-server-plugin-base": "^0.10.4"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "apollo-server": "^2.19.2",
    "apollo-server-testing": "^2.19.2",
    "graphql": "^15.4.0",
    "graphql-query-complexity": "^0.7.2",
    "graphql-tag": "^2.11.0",
    "graphql-tools": "^7.0.2",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "collectCoverage": true,
    "collectCoverageFrom": [
      "src/**/*.ts",
      "!src/**/__tests__/*.ts"
    ],
    "testPathIgnorePatterns": [
      "<rootDir>/node_modules/",
      "<rootDir>/dist/"
    ]
  }
}
